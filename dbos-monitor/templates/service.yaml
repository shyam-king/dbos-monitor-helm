apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbos-monitor.fullname" . }}
  labels:
    {{- include "dbos-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dbos-monitor.selectorLabels" . | nindent 4 }}
